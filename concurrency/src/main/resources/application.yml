spring:
  datasource:
    url: jdbc:mysql://localhost:3306/scheduled-concurrency?createDatabaseIfNotExist=true
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
      format_sql: true